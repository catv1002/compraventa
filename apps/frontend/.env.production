# Vite carga este archivo automáticamente en `vite build` (modo production),
# sin depender de que Cloudflare Workers Builds exponga las "Variables and
# secrets" del dashboard al proceso de build — solo las vimos disponibles en
# el runtime del Worker, no en el build de Vite.
# No es secreto: cualquier VITE_* queda de todas formas expuesto en el bundle
# del navegador, así que no hay problema en commitearlo.
VITE_API_URL="https://compraventa-backend-staging.up.railway.app"
